# ComputableReal 的三大核心挑戰與待解問題

> 本文件整理 `ComputableReal` 目前面臨的核心困難與尚待處理的問題，用於說明系統邊界與後續研究方向。

## 1. 符號運算缺失與等式判定能力之間的張力

`ComputableReal` 的核心表示是 **有理比較 oracle**，主要能力是比較、逼近與區間保證，而不是保存或化簡符號結構。這使系統能維持乾淨的 oracle-based runtime，但也直接帶來一個根本限制：

- 某些數學上顯而易見的等式，**在目前表示法下無法被判定**；
- 問題不只在於效率，而在於結果物件只保留「如何比較」的能力，而不保留代數來源資訊；
- 因此，像 `(\sqrt{2})^2 = 2` 這種顯而易見的代數等式，不能被判定。

這暴露出一條清楚的系統邊界：目前的 `ComputableReal` 偏向 **semantic comparison runtime**，而不是 **symbolic algebra system**。因此，等式判定能力在本質上受到表示法選擇的限制。

### 待解問題

- 在不把系統推向完整 symbolic engine 的前提下，哪些等式類型應被額外辨識？
- 系統是否需要一個比 sign oracle 更強、但又明顯弱於 CAS 的中介層？

---

## 2. 長運算鏈造成的呼叫深度問題

目前 `ComputableReal` 的四則運算，是以 closure 閉包引用上游運算元，形成隱式 DAG。這種設計的優點是自然共享 refinement 資訊，也符合需求驅動的執行模型；但它同時意味著：

- 長表達式會形成深層 sign-function 呼叫鏈；
- 語意上的依賴深度，會直接映射到 Python 執行時的呼叫堆疊；
- 當表達式過長時，問題不再只是變慢，而可能演變成 **stack overflow / recursion depth** 風險；
- 由於目前結果物件不顯式保存完整結構來源，系統也無法在事後把整體計算圖回收成可迭代的評估程序。

換句話說，現在的設計已經具備「圖」的語意，卻仍以「呼叫鏈」的形式執行；這使大規模組合表達式的可擴展性受到執行模型本身的牽制。

### 待解問題

- 是否需要讓結果物件顯式保存某種可重建的來源結構？
- 是否應區分「輕量閉包節點」與「可展平節點」兩種運算結果？
- 長鏈表達式的評估，能否在不破壞現有語意的前提下，轉化為迭代式或分層式執行？

---

## 3. 實數 hash 策略的全域化

目前 `ComputableReal` 的 hash 依賴 `set_max_denominator_for_hash`，而這個設定是**類別／全域層級**的，且只能指定一次。這帶來的問題不是單純 API 使用不便，而是語意層次上的僵硬：

- hash 精度變成整個執行環境共享的全域策略；
- 不同模組、不同資料結構、不同使用情境，無法自然採用不同的 hash 精度語意；
- 一旦某處先設定了分母預算，其他地方就必須接受同一個決策；
- 因而，hash 不再只是物件性質的一部分，而帶有明顯的**程序全域政策**色彩。

對 `ComputableRational` 而言，hash 可以直接建立在 exact value 上；但對 `ComputableReal` 而言，hash 必然涉及近似與分母預算。現在的困難在於：這個近似政策被固定在單一全域旋鈕上，導致局部語意難以共存。

### 待解問題

- hash 的精度語意應屬於類別、物件、容器，還是單次操作？
- 不同使用情境若需要不同的 hash 粗細度，系統應如何共存？
- `ComputableReal` 的 hash，究竟應追求跨情境一致性，還是情境化可配置性？

---

## 總結

上述三項挑戰分別對應到 `ComputableReal` 的三個核心邊界：

1. **表示法邊界**：純 oracle 表示不自帶充分的等式判定能力；
2. **執行模型邊界**：隱式 DAG 與 Python 呼叫堆疊之間存在可擴展性張力；
3. **語意政策邊界**：實數 hash 目前仍仰賴全域設定，難以局部化。

這三點並不是零碎的工程瑕疵，而是目前 `ComputableReal` 作為 oracle-based runtime 在表示、執行與語意政策三個層面上，最值得正面處理的未決問題。
